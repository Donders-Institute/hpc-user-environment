#!/bin/bash
#
# shell script to start Freesurfer/freeview
#
# 20260714 edwger: add condition start from VNC mentat (slurm job) or
#                  within (interactive) OOD VNC (binary start)
# 20170626 edwger: do NOT reload module is one is already loaded
# 20170425 edwger: check for eventually loaded module
# 20170217 edwger: initial wrapperscript

function freeview_executable() {
    [[ "$FREESURFER_HOME" != "" && -x "$FREESURFER_HOME/bin/freeview" ]] &&
        echo "$FREESURFER_HOME/bin/freeview" ||
        which freeview
}

# load helper functions
source $CLUSTER_UTIL_ROOT/share/cluster-lib.sh

mydir=`get_script_dir $0`

ensure_environment_module

SELECTION=$(yad \
    --center \
    --width=300 \
    --ontop \
    --buttons-layout=center \
    --title="Freeview" \
    --text="Select desired Freesurfer/freeview version:" \
    --text-align=center \
    --form \
    --separator=" " \
    --item-separator="\n" \
    --field="Freeview Version:":CB \
    "$(module_avail_versions 'freesurfer' '3.0.3')")
ret=$?
if [[ $ret -eq 1 ]] || [[ $ret -eq 252 ]]
then
    echo "Command Cancelled"; exit 0
fi

IFS=' ' read -r -a CHOICES <<< $SELECTION
FREEVIEW_SELECTION=$(echo ${CHOICES[0]})

echo FREEVIEW-version: $FREEVIEW_SELECTION
echo Loaded-module:  $DEFAULT_VERSION

module unload freesurfer > /dev/null 2>&1

echo "Loading module freesurfer/$FREEVIEW_SELECTION ..."
module load freesurfer/$FREEVIEW_SELECTION

if [ -z $SLURM_JOBID ]; then
    run_guiapp Freesurfer/freeview "$(freeview_executable)" guimenu $@
else
    echo "Starting freeview gui App..."
    "$(freeview_executable)" $@ 2>/dev/null &
fi
